{
  "name": "kcmps-jobs-lambdas",
  "private": true,
  "description": "Dependencies for backend/jobs/*.js — event-driven backend Lambdas (DynamoDB Streams handler, EventBridge-cron expiry sweep), not invoked via API Gateway. Also requires ../lib (backend/lib) alongside on the Lambda's module path.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/util-dynamodb": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0"
  }
}
