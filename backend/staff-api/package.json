{
  "name": "kcmps-staff-api-lambdas",
  "private": true,
  "description": "Dependencies for backend/staff-api/*.js (Milestone 1.3 — staff verification against real data, JWT-authorizer-gated). Deploy each .js file as its own Lambda; also requires ../lib (backend/lib) alongside on the Lambda's module path.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0"
  }
}
