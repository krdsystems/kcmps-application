# Copy to terraform.tfvars (git-ignored) and fill in real values.
# Never commit the filled-in file — it holds live secrets.

# primary_profile = "kcmps-claude-priv"
# dns_profile     = "default"

# google_client_id     = "REPLACE-ME.apps.googleusercontent.com"
# google_client_secret = "REPLACE-ME"

# Only if dev.kcmps.com also needs recovering — see
# storefront-infra/dev-domain.cfn.yaml (separate CloudFormation deploy) and
# ../README.md "Recovering dev.kcmps.com". Its output feeds this variable:
# dev_distribution_domain_name = "<dev-domain.cfn.yaml stack output>"
